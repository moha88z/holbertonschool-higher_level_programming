-- Task 4: Create a table named first_table
-- This script creates first_table with columns id (INT) and name (VARCHAR(256))
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);

